<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->
    <SwitchPreference
            android:key="notifications_reminder_notifications"
            android:icon="@drawable/ic_notifications_black_24dp"
            android:title="@string/pref_title_daily_notifications"
            android:defaultValue="true"/>

    <ListPreference
            android:key="notification_frequency"
            android:icon="@drawable/ic_sync_black_24dp"
            android:title="@string/pref_title_notification_frequency"
            android:entries="@array/pref_notification_frequency_titles"
            android:entryValues="@array/pref_notification_frequency_values"
            android:defaultValue="720000"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"/>

    <SwitchPreference
            android:key="notifications_two_week_reminder_notification"
            android:icon="@drawable/ic_notifications_black_24dp"
            android:title="@string/pref_title_two_week_reminder_notification"
            android:defaultValue="true"/>

</PreferenceScreen>
